# --- BigQuery (server) ---
# Base64-encoded service account JSON for naboo-app-365515.
# Needs BigQuery Job User + read access to the finance datasets
# + BigQuery Data Editor on the finance_ops dataset (annotations).
BIG_QUERY_JSON=""

# --- Google OAuth (server) ---
# OAuth 2.0 Web client from the Naboo Google Cloud console.
# Authorized redirect URIs must include:
#   https://<your-domain>/api/auth/callback
#   http://localhost:5173/api/auth/callback
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Random secret for signing session cookies: openssl rand -base64 32
SESSION_SECRET=""
